\documentclass{standalone}

\begin{document}
\begin{figure}[h] % place it here (h)
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      grid=both,
      grid style={line width=.1pt, draw=gray!30},
      xlabel=$x$, ylabel={$g(x)$}
      ]
      \addplot[very thick, color=funcolor]{1/(1 + exp(x)^(-1))};
    \end{axis}
  \end{tikzpicture}
  \caption{Funzione sigmoidea $\displaystyle g(x) = \frac{1}{1 + e^{-x}}$ }
  \label{fig:sigmoid}
\end{figure}
\end{document}
